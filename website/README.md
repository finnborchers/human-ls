# HUMAN-LS Website

Isolated visual prototype for the Lower Saxony clinic atlas.

## Commands

```bash
npm install
npm run dev
```

The app builds its dataset from the existing repository inputs before starting:

- `../configs/place_urls.json`
- `../artifacts/capture_reviews_run_summary.json`
- `../artifacts/*/reviews.json`
